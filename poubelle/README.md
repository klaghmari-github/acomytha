# Poubelle

Fichiers de reporting, logs de conversion, brouillons. **Pas** un dossier de travail.

Les histoires restent dans `stories/arbres/` (Excel) et `stories/json/` (JSON).  
Le convertisseur écrit `conversion_report.json` ici, pas à côté des JSON.
